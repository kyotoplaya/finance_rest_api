[project]
name = "finance-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kyotoplaya", email = "homarucom@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "sqlalchemy>=2.0.52",
    "uvicorn[standard]>=0.52.4",
]

[project.scripts]
finance-api = "finance_api:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
